JWT_SECRET=your_super_secret_jwt_key_minimum_32_characters_long_please_change_this
DATABASE_URL="postgresql://user:password@localhost:5432/aura_one?schema=public"
PORT=3001
NODE_ENV=development

# CORS Configuration (comma-separated list of allowed origins)
# Example: ALLOWED_ORIGINS=https://app.aura.com,https://admin.aura.com
ALLOWED_ORIGINS=

# Rate Limiting (requests per time window)
THROTTLE_TTL=60
THROTTLE_LIMIT=10
